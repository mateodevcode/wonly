/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["i.postimg.cc", "streamtape.com", "www.blogger.com"],
  },
};

export default nextConfig;

